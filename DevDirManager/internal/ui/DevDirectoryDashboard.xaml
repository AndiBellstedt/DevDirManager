<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="DevDirManager Dashboard" Height="720" Width="1200" MinHeight="640" MinWidth="960" WindowStartupLocation="CenterScreen" ResizeMode="CanResize" Background="{DynamicResource Brush.WindowBackground}">
    <Window.Resources>
        <SolidColorBrush x:Key="Brush.WindowBackground" Color="#FF0F172A" />
        <SolidColorBrush x:Key="Brush.Surface" Color="#FF182030" />
        <SolidColorBrush x:Key="Brush.SurfaceAlt" Color="#FF202A3B" />
        <SolidColorBrush x:Key="Brush.Border" Color="#FF394057" />
        <SolidColorBrush x:Key="Brush.PrimaryText" Color="#FFF5F7FA" />
        <SolidColorBrush x:Key="Brush.SecondaryText" Color="#FFA5ACBC" />
        <SolidColorBrush x:Key="Brush.ButtonText" Color="#FFFFFFFF" />
        <SolidColorBrush x:Key="Brush.Accent" Color="#FF2F6BFF" />
        <SolidColorBrush x:Key="Brush.GridLines" Color="#FF141B2B" />

        <Style TargetType="TabControl">
            <Setter Property="Background" Value="{StaticResource Brush.Surface}" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Margin" Value="0" />
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="{StaticResource Brush.ButtonText}" />
            <Setter Property="Padding" Value="20,10" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Border" Background="{StaticResource Brush.SurfaceAlt}" CornerRadius="8,8,0,0" Padding="0" Margin="0,0,8,0">
                            <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="16,6" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource Brush.Accent}" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Opacity" Value="0.6" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource Brush.PrimaryText}" />
            <Setter Property="Background" Value="{StaticResource Brush.Accent}" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="14,6" />
            <Setter Property="Margin" Value="8,0,0,0" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="{StaticResource Brush.PrimaryText}" />
            <Setter Property="Background" Value="{StaticResource Brush.SurfaceAlt}" />
            <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8" />
            <Setter Property="Margin" Value="0,0,12,0" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="{StaticResource Brush.PrimaryText}" />
            <Setter Property="Background" Value="{StaticResource Brush.SurfaceAlt}" />
            <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="6" />
            <Setter Property="Margin" Value="0,0,12,0" />
            <Setter Property="MinWidth" Value="180" />
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource Brush.PrimaryText}" />
            <Setter Property="Margin" Value="0,8,16,0" />
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="{StaticResource Brush.Surface}" />
            <Setter Property="Foreground" Value="{StaticResource Brush.PrimaryText}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="RowBackground" Value="{StaticResource Brush.Surface}" />
            <Setter Property="AlternatingRowBackground" Value="{StaticResource Brush.SurfaceAlt}" />
            <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource Brush.GridLines}" />
            <Setter Property="VerticalGridLinesBrush" Value="{StaticResource Brush.GridLines}" />
            <Setter Property="ColumnHeaderHeight" Value="34" />
            <Setter Property="AutoGenerateColumns" Value="False" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="CanUserDeleteRows" Value="False" />
            <Setter Property="Margin" Value="0" />
        </Style>

        <Style TargetType="ScrollViewer">
            <Setter Property="Background" Value="Transparent" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource Brush.Surface}" Padding="28,22" BorderThickness="0">
            <StackPanel>
                <TextBlock x:Name="HeaderText" FontSize="24" FontWeight="SemiBold" Foreground="{StaticResource Brush.PrimaryText}" />
                <TextBlock x:Name="SubHeaderText" Margin="0,6,0,0" FontSize="13" Foreground="{StaticResource Brush.SecondaryText}" />
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Margin="0" Background="{StaticResource Brush.WindowBackground}">
            <TabControl x:Name="MainTabControl" Margin="0">
                <TabItem>
                    <TabItem.Header>
                        <TextBlock x:Name="DiscoverTabHeader" />
                    </TabItem.Header>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="28,24">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock x:Name="DiscoverPathLabel" Grid.Column="0" VerticalAlignment="Center" Foreground="{StaticResource Brush.PrimaryText}" />
                                <TextBox x:Name="DiscoverPathBox" Grid.Column="1" MinWidth="320" />
                                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Left">
                                    <Button x:Name="DiscoverBrowseButton" Margin="0,0,0,0" />
                                    <Button x:Name="DiscoverScanButton" Margin="10,0,0,0" />
                                </StackPanel>
                            </Grid>

                            <Border Background="{StaticResource Brush.Surface}" CornerRadius="12" Padding="12" Margin="0,20,0,0">
                                <DataGrid x:Name="DiscoverGrid" GridLinesVisibility="None">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn x:Name="DiscoverRelativePathColumn" Binding="{Binding RelativePath}" Width="220" />
                                        <DataGridTextColumn x:Name="DiscoverRemoteNameColumn" Binding="{Binding RemoteName}" Width="140" />
                                        <DataGridTextColumn x:Name="DiscoverRemoteUrlColumn" Binding="{Binding RemoteUrl}" Width="260" />
                                        <DataGridTextColumn x:Name="DiscoverIsRemoteAccessibleColumn" Binding="{Binding IsRemoteAccessible}" Width="140" />
                                        <DataGridTextColumn x:Name="DiscoverUserNameColumn" Binding="{Binding UserName}" Width="180" />
                                        <DataGridTextColumn x:Name="DiscoverUserEmailColumn" Binding="{Binding UserEmail}" Width="220" />
                                        <DataGridTextColumn x:Name="DiscoverStatusDateColumn" Binding="{Binding StatusDate}" Width="160" />
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Border>

                            <TextBlock x:Name="DiscoverSummaryText" Margin="0,12,0,0" Foreground="{StaticResource Brush.SecondaryText}" />
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <TabItem>
                    <TabItem.Header>
                        <TextBlock x:Name="ExportTabHeader" />
                    </TabItem.Header>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="28,24">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock x:Name="ExportFormatLabel" Grid.Column="0" VerticalAlignment="Center" Foreground="{StaticResource Brush.PrimaryText}" />
                                <ComboBox x:Name="ExportFormatCombo" Grid.Column="1" />
                            </Grid>

                            <Grid Margin="0,16,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock x:Name="ExportPathLabel" Grid.Column="0" VerticalAlignment="Center" Foreground="{StaticResource Brush.PrimaryText}" />
                                <TextBox x:Name="ExportPathBox" Grid.Column="1" MinWidth="320" />
                                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Left">
                                    <Button x:Name="ExportBrowseButton" Margin="0,0,0,0" />
                                    <Button x:Name="ExportRunButton" Margin="10,0,0,0" />
                                </StackPanel>
                            </Grid>

                            <Border Background="{StaticResource Brush.Surface}" CornerRadius="12" Padding="12" Margin="0,20,0,0">
                                <DataGrid x:Name="ExportPreviewGrid" GridLinesVisibility="None">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn x:Name="ExportRelativePathColumn" Binding="{Binding RelativePath}" Width="220" />
                                        <DataGridTextColumn x:Name="ExportRemoteUrlColumn" Binding="{Binding RemoteUrl}" Width="260" />
                                        <DataGridTextColumn x:Name="ExportIsRemoteAccessibleColumn" Binding="{Binding IsRemoteAccessible}" Width="160" />
                                        <DataGridTextColumn x:Name="ExportStatusDateColumn" Binding="{Binding StatusDate}" Width="160" />
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Border>

                            <TextBlock x:Name="ExportStatusText" Margin="0,12,0,0" Foreground="{StaticResource Brush.SecondaryText}" />
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <TabItem>
                    <TabItem.Header>
                        <TextBlock x:Name="ImportTabHeader" />
                    </TabItem.Header>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="28,24">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock x:Name="ImportPathLabel" Grid.Column="0" VerticalAlignment="Center" Foreground="{StaticResource Brush.PrimaryText}" />
                                <TextBox x:Name="ImportPathBox" Grid.Column="1" MinWidth="320" />
                                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Left">
                                    <Button x:Name="ImportBrowseButton" Margin="0,0,0,0" />
                                    <Button x:Name="ImportLoadButton" Margin="10,0,0,0" />
                                </StackPanel>
                            </Grid>

                            <Border Background="{StaticResource Brush.Surface}" CornerRadius="12" Padding="12" Margin="0,20,0,0">
                                <DataGrid x:Name="ImportGrid" GridLinesVisibility="None">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn x:Name="ImportRelativePathColumn" Binding="{Binding RelativePath}" Width="220" />
                                        <DataGridTextColumn x:Name="ImportRemoteUrlColumn" Binding="{Binding RemoteUrl}" Width="260" />
                                        <DataGridTextColumn x:Name="ImportIsRemoteAccessibleColumn" Binding="{Binding IsRemoteAccessible}" Width="160" />
                                        <DataGridTextColumn x:Name="ImportUserNameColumn" Binding="{Binding UserName}" Width="180" />
                                        <DataGridTextColumn x:Name="ImportUserEmailColumn" Binding="{Binding UserEmail}" Width="220" />
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Border>

                            <TextBlock x:Name="ImportSummaryText" Margin="0,12,0,0" Foreground="{StaticResource Brush.SecondaryText}" />
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <TabItem>
                    <TabItem.Header>
                        <TextBlock x:Name="RestoreTabHeader" />
                    </TabItem.Header>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="28,24">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock x:Name="RestoreListPathLabel" Grid.Column="0" VerticalAlignment="Center" Foreground="{StaticResource Brush.PrimaryText}" />
                                <TextBox x:Name="RestoreListPathBox" Grid.Column="1" MinWidth="320" />
                                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Left">
                                    <Button x:Name="RestoreListBrowseButton" Margin="0,0,0,0" />
                                </StackPanel>
                            </Grid>

                            <Grid Margin="0,16,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock x:Name="RestoreDestinationLabel" Grid.Column="0" VerticalAlignment="Center" Foreground="{StaticResource Brush.PrimaryText}" />
                                <TextBox x:Name="RestoreDestinationBox" Grid.Column="1" MinWidth="320" />
                                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Left">
                                    <Button x:Name="RestoreDestinationBrowseButton" Margin="0,0,0,0" />
                                    <Button x:Name="RestoreRunButton" Margin="10,0,0,0" />
                                </StackPanel>
                            </Grid>

                            <WrapPanel Margin="0,20,0,0" HorizontalAlignment="Left">
                                <CheckBox x:Name="RestoreForceCheckBox" />
                                <CheckBox x:Name="RestoreSkipExistingCheckBox" />
                                <CheckBox x:Name="RestoreShowGitOutputCheckBox" />
                                <CheckBox x:Name="RestoreWhatIfCheckBox" />
                            </WrapPanel>

                            <Border Background="{StaticResource Brush.Surface}" CornerRadius="12" Padding="12" Margin="0,20,0,0">
                                <DataGrid x:Name="RestoreGrid" GridLinesVisibility="None">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn x:Name="RestoreRelativePathColumn" Binding="{Binding RelativePath}" Width="220" />
                                        <DataGridTextColumn x:Name="RestoreRemoteUrlColumn" Binding="{Binding RemoteUrl}" Width="260" />
                                        <DataGridTextColumn x:Name="RestoreIsRemoteAccessibleColumn" Binding="{Binding IsRemoteAccessible}" Width="160" />
                                        <DataGridTextColumn x:Name="RestoreStatusDateColumn" Binding="{Binding StatusDate}" Width="160" />
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Border>

                            <TextBlock x:Name="RestoreSummaryText" Margin="0,12,0,0" Foreground="{StaticResource Brush.SecondaryText}" />
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <TabItem>
                    <TabItem.Header>
                        <TextBlock x:Name="SyncTabHeader" />
                    </TabItem.Header>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="28,24">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock x:Name="SyncDirectoryLabel" Grid.Column="0" VerticalAlignment="Center" Foreground="{StaticResource Brush.PrimaryText}" />
                                <TextBox x:Name="SyncDirectoryBox" Grid.Column="1" MinWidth="320" />
                                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Left">
                                    <Button x:Name="SyncDirectoryBrowseButton" Margin="0,0,0,0" />
                                </StackPanel>
                            </Grid>

                            <Grid Margin="0,16,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock x:Name="SyncListPathLabel" Grid.Column="0" VerticalAlignment="Center" Foreground="{StaticResource Brush.PrimaryText}" />
                                <TextBox x:Name="SyncListPathBox" Grid.Column="1" MinWidth="320" />
                                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Left">
                                    <Button x:Name="SyncListBrowseButton" Margin="0,0,0,0" />
                                    <Button x:Name="SyncRunButton" Margin="10,0,0,0" />
                                </StackPanel>
                            </Grid>

                            <WrapPanel Margin="0,20,0,0" HorizontalAlignment="Left">
                                <CheckBox x:Name="SyncForceCheckBox" />
                                <CheckBox x:Name="SyncSkipExistingCheckBox" />
                                <CheckBox x:Name="SyncShowGitOutputCheckBox" />
                                <CheckBox x:Name="SyncWhatIfCheckBox" />
                            </WrapPanel>

                            <Border Background="{StaticResource Brush.Surface}" CornerRadius="12" Padding="12" Margin="0,20,0,0">
                                <DataGrid x:Name="SyncGrid" GridLinesVisibility="None">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn x:Name="SyncRelativePathColumn" Binding="{Binding RelativePath}" Width="220" />
                                        <DataGridTextColumn x:Name="SyncRemoteUrlColumn" Binding="{Binding RemoteUrl}" Width="260" />
                                        <DataGridTextColumn x:Name="SyncIsRemoteAccessibleColumn" Binding="{Binding IsRemoteAccessible}" Width="160" />
                                        <DataGridTextColumn x:Name="SyncStatusDateColumn" Binding="{Binding StatusDate}" Width="160" />
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Border>

                            <TextBlock x:Name="SyncSummaryText" Margin="0,12,0,0" Foreground="{StaticResource Brush.SecondaryText}" />
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>
            </TabControl>
        </Border>

        <StatusBar Grid.Row="2" Background="{StaticResource Brush.Surface}" Foreground="{StaticResource Brush.PrimaryText}" Padding="24,6">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
